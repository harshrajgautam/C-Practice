#include<stdio.h>
int main(){
    long int distance,speed,time;
    printf("distance in m is :-");
    scanf("%d",&distance);
    printf("speed is:-");
    scanf("%d",&speed);
    printf("time in second =%u",time=distance/speed);
    return 0;
}
